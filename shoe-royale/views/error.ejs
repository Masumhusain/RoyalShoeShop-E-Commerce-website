<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error | Royal Footwear</title>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    
    <style>
        .error-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            padding: 20px;
            color: white;
        }
        
        .error-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 40px;
            max-width: 600px;
            width: 100%;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .error-header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .error-icon {
            font-size: 4rem;
            color: #ff6b6b;
            margin-bottom: 20px;
        }
        
        .error-title {
            font-size: 2.5rem;
            margin-bottom: 10px;
            color: white;
        }
        
        .error-subtitle {
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 30px;
        }
        
        .error-details {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 30px;
            font-family: monospace;
            font-size: 0.9rem;
            max-height: 200px;
            overflow-y: auto;
        }
        
        .error-actions {
            display: flex;
            gap: 15px;
            justify-content: center;
        }
        
        .btn-error {
            padding: 12px 30px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        
        .btn-error-primary {
            background: #ffd700;
            color: #1a1a2e;
        }
        
        .btn-error-primary:hover {
            background: #ffed4e;
            transform: translateY(-2px);
            color: #1a1a2e;
        }
        
        .btn-error-secondary {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        
        .btn-error-secondary:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
            color: white;
        }
        
        @media (max-width: 768px) {
            .error-card {
                padding: 20px;
            }
            
            .error-actions {
                flex-direction: column;
            }
            
            .btn-error {
                width: 100%;
                text-align: center;
            }
        }
    </style>
</head>
<body class="error-container">
    <div class="error-card">
        <div class="error-header">
            <div class="error-icon">
                <i class="fas fa-bug"></i>
            </div>
            <h1 class="error-title">Something Went Wrong</h1>
            <p class="error-subtitle">
                We apologize for the inconvenience. Our team has been notified.
            </p>
        </div>
        
        <!-- Error Message -->
        <div class="error-details">
            <% if (message) { %>
                <p><strong>Error:</strong> <%= message %></p>
            <% } %>
            
            <% if (error && error.message) { %>
                <p><strong>Details:</strong> <%= error.message %></p>
            <% } %>
            
            <% if (error && error.stack && process.env.NODE_ENV === 'development') { %>
                <hr>
                <p><strong>Stack Trace:</strong></p>
                <pre style="color: #ff6b6b; white-space: pre-wrap;"><%= error.stack %></pre>
            <% } %>
        </div>
        
        <!-- Actions -->
        <div class="error-actions">
            <a href="/" class="btn-error btn-error-primary">
                <i class="fas fa-home me-2"></i>Go to Homepage
            </a>
            
            <a href="javascript:history.back()" class="btn-error btn-error-secondary">
                <i class="fas fa-arrow-left me-2"></i>Go Back
            </a>
            
            <button onclick="location.reload()" class="btn-error btn-error-secondary">
                <i class="fas fa-redo me-2"></i>Reload Page
            </button>
        </div>
        
        <!-- Support Info -->
        <div class="mt-4 text-center">
            <p class="small" style="color: rgba(255, 255, 255, 0.7);">
                If the problem persists, please contact our support team.
                <br>
                <a href="mailto:support@royalfootwear.com" class="text-gold">support@royalfootwear.com</a>
            </p>
        </div>
    </div>
    
    <script>
        // Log error to console for debugging
        console.error('Page Error:', '<%= message %>');
        
        // Auto-reload after 30 seconds if user is still on page
        setTimeout(() => {
            if (confirm('The page seems to be stuck. Would you like to reload?')) {
                location.reload();
            }
        }, 30000);
    </script>
</body>
</html>